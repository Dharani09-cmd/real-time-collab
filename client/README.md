# Client (React)

- Start with `npm install` and `npm start`.
- Defaults: backend socket server `http://localhost:5000`. To change, set REACT_APP_SOCKET_URL in client/.env

Note: This is a minimal starter; in production you'd add auth, routing protections, and better build configs.
